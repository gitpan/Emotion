context and closing must accept multiple situations (answer too?)

convert left/right -> L/R

echo
  impasse id=a1
  impasse echo=a1 absent=right

merge prepare_* into a single command-line tool `emotion'?

duration index

rename "context"?
